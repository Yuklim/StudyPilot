[alembic]
script_location = %(here)s/migrations
prepend_sys_path = %(here)s/src
path_separator = os
# The URL is supplied by infrastructure Settings (STUDYPILOT_DATABASE_URL),
# never duplicated here or logged. Run documented commands from backend/.
