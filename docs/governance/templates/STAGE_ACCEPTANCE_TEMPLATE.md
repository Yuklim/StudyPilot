# TASK-XXX L3 验收（L1/L2 不创建）

默认写入单任务 EVIDENCE 区，复杂/遗留证据才另存。
- Integration 身份、实际只读权限证据：
- candidate / evidence HEAD / 对应 Review：
- 完成条件 → 真实证据（只列映射，不复制测试日志）：
- 候选至 evidence HEAD 是否仅窄证据写回：
- 未解决风险/缺口：
- 结论：PASS / RETURN / BLOCKED；下一步：

不要再次完整审代码或默认重跑测试；只补缺失且必要的跨模块验证。禁止修改、提交、解决冲突或合并。PASS 不等于用户已合并。
