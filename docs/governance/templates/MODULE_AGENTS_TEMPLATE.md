# <模块名称> Agent 规则

## 1. 适用范围

本文件仅适用于 `<module/path/**>`，并补充仓库根 `AGENTS.md`。不得重复或放宽根规则。

## 2. 模块职责

- 本模块负责：
- 本模块不负责：
- 上游依赖：
- 下游使用者：

## 3. 必须保持的不变量

- 描述后果明显、无法完全依赖自动检查的重要边界。
- 同时说明安全的修改路径或兼容方式。

## 4. 公共契约

- 契约文档位置：
- 未经批准不得改变的外部行为：

## 5. 准确命令

只有命令已经在当前仓库验证后才填写。

```text
安装：
格式化：
静态检查：
模块测试：
集成测试：
构建：
```

## 6. 文件边界

- 默认允许：
- 默认禁止：
- 生成文件：
- 迁移或锁文件要求：

## 7. Code Review Rules

### <重要规则名称>

- 要发现的违规行为：
- 真实风险：
- 安全路径：

不要把可由 formatter、lint 或测试机械发现的问题重复写成审查规则。
