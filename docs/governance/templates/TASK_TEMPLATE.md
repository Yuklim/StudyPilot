# TASK-XXX：任务名称

## 1. 基本信息

- 状态：`DRAFT`
- 负责人角色：
- 创建人：
- 创建日期：
- 目标阶段：
- 基线分支或提交：

## 2. 背景与依据

说明该任务来自哪一项已确认需求，并链接对应文档位置。

## 3. 目标

用可观察结果描述本任务完成后用户或系统能够做到什么。

## 4. 非目标

- 明确本任务不处理的内容。
- 列出容易被误认为属于本任务的相邻功能。

## 5. 允许修改路径

- `path/to/owned/**`

路径不存在时，应在任务中明确是否允许创建。

任务单和以下证据文件由 `coordinator` 在控制面中维护，不属于开发 Agent 的默认写入范围：

```text
docs/tasks/TASK-XXX-<名称>.md
docs/tasks/TASK-XXX-HANDOFF.md
docs/tasks/TASK-XXX-REVIEW.md
docs/tasks/TASK-XXX-ACCEPTANCE.md
```

## 6. 禁止修改路径

- `path/to/not-owned/**`
- 产品需求范围，除非负责人角色是 `requirements_owner`
- 公共契约，除非任务明确包含已批准的契约变更

## 7. 前置条件与依赖

- [ ] 需求已确认
- [ ] 任务边界已确认
- [ ] 公共契约已确认或本任务不涉及公共契约
- [ ] 依赖任务已经完成
- [ ] 允许路径没有被其他活跃任务占用

## 8. 功能要求

1. `<填写功能要求>`
2. `<填写功能要求>`

## 9. 验收条件

- [ ] 用户可观察行为符合需求
- [ ] 只修改允许路径
- [ ] 对应测试或验证已完成
- [ ] 文档和契约按需更新
- [ ] 不存在未说明的已知缺陷
- [ ] 已提交标准交接报告

## 10. 必须执行的检查

技术栈确定后填写准确命令。若尚未确定，写明“当前无可执行技术检查”，不得猜测命令。

```text
<command>
```

## 11. 审查要求

- Reviewer 角色：`qa_reviewer`
- 审查重点：需求、范围、正确性、安全、测试
- 是否需要额外专项审查：

## 12. 交接要求

使用 `HANDOFF_TEMPLATE.md`，把报告返回给 `coordinator`，由协调者保存为 `docs/tasks/TASK-XXX-HANDOFF.md`，并提供：

- 完整修改清单；
- 实际检查证据；
- 分支名与提交 SHA；
- 已知风险与未完成事项。

## 13. 决策与状态记录

只有 `coordinator` 可以维护本节状态；其他角色只能返回状态建议和证据。

| 日期 | 状态或决定 | 责任人 | 说明 |
| --- | --- | --- | --- |
| YYYY-MM-DD | DRAFT |  |  |
