# TASK-084：认出是文献就直接存，确认页不再让用户勾两个框

```toml
schema_version = 2
id = "TASK-084"
status = "ACCEPTED"
risk = "L2"
risk_reason = "前端单页改动，不动契约、不动数据含义、不动后端。不命中 risk-policy.json 的 high_risk_paths。不定 L1 的理由是它**移除用户控制**：原本两个勾选框各自是一条退路（改存网页正文、不存文献信息），去掉之后行为不可逆地由程序决定，需要一位独立 Reviewer 核对「被去掉的那两条路是否真的还有别的出口」以及失败分支是否被顺带改坏。执行链：1 Worker → 自动检查 → 1 独立只读 Reviewer。"
risk_flags = ["business"]
owner = "coordinator"
base = "168830e5b2947f92677bea1dbd18489b30f51512"
allowed_paths = [
  "frontend/src/features/capture/CapturePage.tsx",
  "frontend/src/features/capture/CapturePage.test.tsx",
  "frontend/src/styles.css",
  "docs/tasks/TASK-084-capture-simplify.md",
]
checks = ["frontend"]
```

## 需求与范围

### 用户授权

2026-09-21 用户：「现在爬取文献之后保存页的信息有点繁杂，现在有『保存这份 pdf』和
『这页看起来是一篇文献，一并存下来』两个选项，有什么区别？**如果是文献的话默认保存 pdf 原文
就可以了，不要让用户做太多选择。**」

2026-09-21 主 Agent 就「跳转保存」的两种读法提问（直接写入库 vs 确认页只剩一个按钮），
用户定案：**「保留确认页，做成你上面说的这个效果」**，并给出目标形状：

```
来自：https://arxiv.org/abs/1706.03762
这次会存下：PDF 原件（1706.03762.pdf，2.1 MB）和下面这份文献信息。
            打开资料就能在 StudyPilot 里读。

标题  [Attention Is All You Need        ]

类型  预印本
作者  Ashish Vaswani；Noam Shazeer；…
年份  2017
DOI   10.48550/arXiv.1706.03762

           [ 保存为资料 ]
```

**因此契约第 14.3 节不动**：「写入只发生在用户点击『保存为资料』之后」这条保持原样，
本任务仍是 L2。提问的缘由是另一种读法（点一下图标就直接写进库）会撞上该节明写的
「不得为『自动保存更顺手』而取消」，那需要升 L3 并改契约——用户选择不走那条路。

**本次不含**：用户同一轮还提到「爬取失败时在插件页反馈失败、再退回爬取正文」。
那是扩展 popup 的改动（另一组文件、另有一个未定的设计问题：popup 在确认页标签页打开的瞬间
就被浏览器关掉，要在它上面显示反馈得先解决这个），**不并入本任务**，另行登记。

### 现状（登记前核实）

认出文献且抓到 PDF 时，`/capture` 确认页从上到下是：

1. `来自：<原页地址>`
2. **勾选框「保存这份 PDF（xxx.pdf，2.1 MB）」** + 一整段解释（勾/不勾两种文案）
3. `标题` 输入框
4. **勾选框「这页看起来是一篇文献，一并存下来」** + 类型/作者/年份/出处/DOI 的明细表
5. 「保存为资料」按钮

两个勾选框管的是**两件不同的事**，但它们是分两次长出来的，用户看不出区别：

- 「保存这份 PDF」（TASK-080）决定**存什么内容**：勾＝PDF 原件存成 FILE 资料；不勾＝改存网页正文。
- 「这页看起来是一篇文献，一并存下来」（TASK-075）决定**存不存文献元数据**（作者/年份/DOI），
  与上一条正交。TASK-075 落地时还没有抓 PDF 这回事。

两者默认都勾着，且对一篇论文来说答案几乎永远是「都要」。

### 目标

1. **认出文献且抓到 PDF 时，不再要求用户做任何勾选**：直接存 PDF 原件 + 文献信息，
   确认页只保留「这次会存下什么」的**一句陈述**、文献明细表（供核对是不是这篇）、标题框、保存按钮。
2. **认出文献但没抓到 PDF 时**（跨域/付费墙/超限/超时），同样不再要求勾选文献信息：
   存网页正文 + 文献信息，并保留既有的「为什么没拿到 PDF」提示。
3. **没认出文献的普通网页：行为一字不变**。这是最容易误伤的地方，须有用例钉住。
4. 失败处置不变：文献信息写失败时仍停在本页、如实说明、给「打开这份资料」的出口。

### 非目标 / 禁止范围

- 不改抓取逻辑、不改契约、不改扩展。
- 不改「连图片一并保存」那条既有路径。
- 不动资料详情页的「信息」Tab（文献信息事后修改的出口在那里，本任务依赖它但不改它）。
- **不写 `docs/tasks/任务索引.md`**：TASK-083 未合并且持有索引，本任务索引行并入下一个已授权任务
  的控制面提交（AGENTS.md §3/§5 与用户既定指令）。

### 主 Agent 登记的决定与取舍（用户给了方向，这几条是我定的具体形状，Review 可挑战）

- **两个勾选框都去掉，而不是只去掉一个**。用户说的是「不要让用户做太多选择」，只去掉一个
  仍然留着一个语义含糊的框。
- **被去掉的两条退路，代价明示**：
  - 「不存文献信息」→ **仍有出口**：存完之后在资料详情页右栏「信息」Tab 可以改或清空（TASK-078）。
  - 「改存网页正文而不是 PDF」→ **没有等价出口**，只能重新采集（而重新采集还是会存 PDF）。
    判断：用户在 TASK-080 定案时明说「原文页一般都是对文献的描述，没用」，此路本就是
    为极少数情形留的后门；为它保留一个每次都要看一眼的勾选框不值得。**这一条如实登记，
    若 Review 或用户不同意，改回一个「改存网页正文」的次要入口即可。**
- **文献明细表保留**：它不是选择，是**核对**——让用户一眼看出认出来的是不是这篇论文。

## 完成条件

- 文献 + 有 PDF：确认页上**没有任何勾选框**；有一句话说清会存 PDF 原件与文献信息；
  保存后产物与改前勾选两个框时**完全一致**（FILE 资料 + 原件 + 文献信息）。有用例。
- 文献 + 没 PDF：没有勾选框；存 WEB 资料 + 正文快照 + 文献信息；「为什么没拿到 PDF」提示仍在。有用例。
- 普通网页（未认出文献）：确认页与改前逐项一致。有用例，且做变异验证。
- 文献信息写失败时的处置不变。有用例。
- `check_task.py` 必要检查 PASS（frontend）。
- L2：1 位独立只读 Reviewer 审最终 diff。
- **按用户 2026-09-21 的指令：本次不主动推 PR**，先交给用户检查，确认后再推。

## 上下文包

- 实现：`frontend/src/features/capture/CapturePage.tsx`（`savePdf`/`saveCitation` 两个 state
  与 `.capture-pdf`/`.capture-citation` 两个区块）、`styles.css` 的同名规则。
- 事后修改文献信息的出口：资料详情页右栏「信息」Tab（TASK-078）。
- 检查：`python3 scripts/governance/check_task.py --task docs/tasks/TASK-084-capture-simplify.md --worktree`。

## 实现与测试

- 实现 SHA：登记 `8bb3146`；实现 `a8d6a81`；Review 修正 `0e66d07`。
- 命令与结果：`check_task.py` → **CHECKS PASS**（5 条，frontend 组）；前端单测 **793 项**全绿。

### 落点

- 删掉 `savePdf` 与 `saveCitation` 两个 state 及其勾选框。抓到 PDF 就存 PDF，认出文献就存文献信息。
- PDF 那块换成一句陈述（`.capture-plan`）：「这次会存下：**PDF 原件**（名字，大小）和下面这份文献信息。
  打开资料就能在 StudyPilot 里读，这一页的正文不会保存——它只是对这篇文献的描述。」
- 文献明细表**保留**，抬头改为陈述：有 PDF 时「这篇文献的信息：」，无 PDF 时「这页看起来是一篇文献，
  会一并存下来：」。它不是选择，是让用户一眼核对认出来的是不是这篇。
- **页面大标题跟着产物变**：存 PDF 时是「确认要保存的**文献**」，否则仍是「确认要保存的正文」。
  这一处是**实测截图时才发现的**——标题写着「正文」而正文下面那句写着「这一页的正文不会保存」，
  两句当场打架。单测断言的是文字内容，看不出这种整页读起来的矛盾。

### 实测（真实 Edge + 真实 arXiv 载荷）

用构建后的注入脚本在真实 `arxiv.org/abs/1706.03762` 上采到真载荷（2.11 MB PDF + PREPRINT 文献），
再投进真实 `/capture` 页：

| 情形 | 页面标题 | 勾选框 | 正文框 |
| --- | --- | --- | --- |
| 论文 + 抓到 PDF | **确认要保存的文献** | **0** | 不显示 |
| 论文 + 没抓到 PDF（`failed`） | 确认要保存的正文 | **0** | 显示，含失败原因与文献明细 |
| 普通网页（非文献） | 确认要保存的正文 | 0 | 显示，与改前一致 |

### Review 之后的修正（独立 Review F2/F3/F4）

- **F2**：`.capture-pdf`、`.capture-pdf .resource-hint`、`.capture-citation-toggle` 三条 CSS 规则
  已无使用方，连同两条与现状不符的注释一并删除。
- **F3**：两处 `queryByRole('checkbox', { name: /保存这份 PDF/ })` 在「无 PDF」场景下改前改后恒为
  null，对本次改动没有判别力（其中一处的载荷其实带着 citation、改前确有「一并存下来」那个框）。
  去掉 `name` 过滤，改为断言**没有任何勾选框**。
- **F4**：`/一并存下来$/` 这条判据依赖抬头结尾的一个全角冒号，改个标点就假红／假绿。
  换成结构判据：断言文献那一块里没有任何 `input/button/select`。

### 用例

改了四条原本钉勾选框的用例，改成钉**新行为**：
- 文献明细仍显示、仍照存，但**没有勾选框**；
- 「确认页上不再有任何可关的开关」（防止日后有人又加回来一个）；
- PDF 那句陈述带文件名、且无勾选框、无正文框；
- 一定走 multipart 上传那条路。
另新增对页面标题的断言。普通网页那条既有用例原样保留，它正是「非文献页一字不变」的守卫。

### 已知限制 / 未完成项

- **「改存网页正文而不是 PDF」这条路没有了，且没有等价出口**（只能重新采集，而重新采集仍会存 PDF）。
  这是本任务明知的取舍，理由见「主 Agent 登记的决定与取舍」。
- 「不存文献信息」这条路也没有了，但**下游有出口**：资料详情页右栏「信息」Tab 可改可清空。
- **扩展 popup 未动**：用户同一轮提到的「爬取失败时在插件页反馈、再退回爬取正文」不在本任务，
  另行登记；那里还有一个已查明但未修的问题——popup 在确认页标签页打开的瞬间就被浏览器关掉，
  TASK-080 给它加的 PDF 回执因此从来显示不出来。

### 必须在下一个任务修掉的陈旧契约（独立 Review F1）

**契约 `docs/contracts/API与数据契约基线.md` 第 14.4 节仍写着**：

> 用户若在确认页**取消勾选**、改存网页正文，那一次的图片保留原网站地址……确认页在取消勾选
> 的那一刻如实说明。

**这句话现在是假的**——那个勾选框已被本任务删掉。它是 TASK-080 时由主 Agent 亲手写进契约的，
本任务把行为改了却没同步它。`extension/src/popup/popup.ts` 里 `shouldAskAboutImages` 的同句注释
同样过期。

**本任务不改它的理由**：`docs/contracts/**` 命中 risk-policy.json 的 high_risk_paths，改它要把本任务
从 L2 升到 L3（多一道独立验收），而本任务的代码改动本身不需要那个级别。按独立 Reviewer 的建议
如实登记、另行授权——**下一个任务（扩展 popup 的反馈与退回）必然要动 §14.4 的同一片区域，
届时一并修正**。在那之前，契约的这一段与实现不一致，**这是已知的、写明的不一致，不是遗漏**。

<!-- EVIDENCE:BEGIN -->
## 状态与最终证据

- **最终候选 SHA**：`0e66d07`（首轮候选 `a8d6a81`，基线 `168830e`）。
- **Review**：L2 独立只读 Reviewer 一轮，**PASS，无必须修复项**；1 条必须登记（F1）+ 3 条可选（F2–F4，均已修）。
  报告核心原文：

> 1. **出口核实（不采信记录）**：`ResourceDetail.tsx:572-582,627-634` → `ResourceToolbar.tsx:542-572` →
>    `:570 <ReaderCitation resourceId={resource.id} />`，**不按 `source_type` 分支**；PDF 资料只去掉「高亮」Tab，
>    「信息」Tab 保留。**出口属实且对 FILE/PDF 可达**。「改存网页正文」确无等价出口：popup 无该选项、
>    `extract.ts` 只要认出文献且同域就抓、确认页无入口；只剩重新采集（仍存 PDF）或手工建 WEB 资料粘贴。
>    记录如实登记，**未轻描淡写**。
> 2. **两条路未被改坏**：非文献页渲染与 `save()` 逐项未变。文献无 PDF：`pdfProblemText` 五条在、提示在、
>    正文框/图片冻结/失败分因均在 WEB 分支未动。
> 3. **`savingNow` 三处自洽**：heading / 副本提示 / 正文框 与 `save()` 的 `savingPdf` 同一判据。
> 4. **citation 失败处置正确**：PDF 路与 WEB 路两处都在，与图片失败并存时两条提示都在、链接只一个。
> 5. **用例有判别力**：`queryByRole('checkbox')` 各处均先确认区块已渲染，且改前这些场景**确有**勾选框 → 非恒真。
>
> **F1（非阻断，但必须登记）** 契约 §14.4 仍写「用户若在确认页**取消勾选**、改存网页正文……」，
> 该控制已被删，这段契约陈述现在是假的。契约不在 allowed_paths → 请在已知限制如实登记并另行授权修订。
> **F2（可选）** 三条 CSS 规则全仓已无使用方，两条注释与现状不符。
> **F3（可选）** 两处 `queryByRole('checkbox', { name: /保存这份 PDF/ })` 在「无 PDF」场景恒为 null，无判别力。
> **F4（记录）** `/一并存下来$/` 依赖抬头结尾的全角冒号，改一个标点即假红/假绿。
>
> 记录「标题跟着产物变是实测才发现、单测看不出整页矛盾」的自省与代码注释一致，**属实**。
>
> **结论：PASS**（无必须修复项；F1 须在记录中如实登记后继续）

F2/F3/F4 已修（见上文「Review 之后的修正」），F1 已登记为「必须在下一个任务修掉的陈旧契约」。

- **最终状态：ACCEPTED**，等待用户合并。**用户已于 2026-09-21 亲自检查确认页后答「可以」**
  （按其当日新指令：先给他检查、确认后才推 PR）。
- **剩余风险**：① 契约 §14.4 与 popup 注释的陈旧文字（F1，下个任务修）；
  ② 「改存网页正文」永久移除且无等价出口（已登记，用户可随时推翻）；
  ③ PDF 确认页没有 e2e，只有单测 + 真实 Edge 实测。
- **非阻断遗留项**：见「已知限制 / 未完成项」。
- **索引行未写**：TASK-083（PR #91）未合并且持有 `任务索引.md`，本任务索引行并入下一个已授权任务的
  控制面提交（AGENTS.md §3/§5 与用户既定指令）。
- 日期与决定日志：2026-09-21 用户问两个勾选框有什么区别、并要求「是文献就默认存 PDF 原文，
  不要让用户做太多选择」→ 主 Agent 核实两框分属 TASK-075 与 TASK-080、语义正交但对论文而言答案
  恒为「都要」→ 登记 TASK-084。
<!-- EVIDENCE:END -->
