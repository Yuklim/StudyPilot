# TASK-083：PDF 占位页被 flex 压扁，导致后半本永远不渲染

```toml
schema_version = 2
id = "TASK-083"
status = "MERGED"
risk = "L2"
risk_reason = "改动本体只有一条 CSS 声明，但它修的是阅读器**滚动几何**的地基：占位页被压扁后，JS 侧按「每页高 + 间距」算出的偏移量与真实 DOM 对不上，连带影响页码判定、跳页与位置记忆。改一行的同时要验这三样是否随之回正，需要一位独立 Reviewer 核对「是否真的只修了这一处、没有别的东西在依赖那个错误的几何」。不命中 risk-policy.json 的 high_risk_paths。执行链：1 Worker → 自动检查 → 1 独立只读 Reviewer。"
risk_flags = ["business"]
owner = "coordinator"
base = "168830e5b2947f92677bea1dbd18489b30f51512"
allowed_paths = [
  "frontend/src/features/resources/PdfReader.tsx",
  "frontend/src/features/resources/PdfReader.test.tsx",
  "frontend/src/styles.css",
  "frontend/e2e/pdf-reader.spec.ts",
  "docs/tasks/TASK-082-pdf-crisp.md",
  "docs/tasks/TASK-083-pdf-page-boxes.md",
  "docs/tasks/任务索引.md",
  # 范围修订 1（2026-09-21，写入前登记）：现有夹具只有 2 页、且两页都在渲染窗口内，
  # 压不出这个缺陷；需要一份页数足够多的合成 PDF 才能让 e2e 真正看住它。
  "frontend/e2e/fixtures/sample-long.pdf",
]
checks = ["frontend"]
```

## 需求与范围

### 用户授权

2026-09-21 用户：「（这份 PDF）**同时只能出现前 7 页内容**。告诉我是怎么回事，不要急着开始做。」
主 Agent 查清根因并汇报后，用户：「已合并，……**解决新任务吧**。」

### 根因（实测，不是推断）

`.pdf-reader-pages` 是 `display: flex; flex-direction: column`，而 flex 子项默认 `flex-shrink: 1`。
内容总高超过容器时浏览器会**压扁**子项：已渲染的页里有 canvas 撑着不缩，**没渲染的占位页里
只有一个页码文字，于是塌成 17px**——而它本该是 792px。

「先量出每页尺寸、占好位子」这句设计（TASK-073）因此在**没渲染的那些页上完全失效**。

真实 Edge 实测（1440×900，`deviceScaleFactor: 2`，15 页的 `1706.03762`）：

```
各页高度: [791, 791, 791, 17, 17, 17, 17, 17, 17, 17, 17, 17, 17, 17, 17]
内容总高: 2834px   ← 应为 12136px（可滚距离 2009px ← 应为 11311px）
滚到底时页码显示: 3   ← 应为 15
```

连锁后果就是用户看到的现象：占位页没占住位置 → 总滚动高度只有该有的约四分之一 →
滚到底时程序以为在第 3 页 → 「只渲染视口上下各 2 页」这条规则**永远算不到第 7 页之后** →
后 8 页一辈子不会画出来。

**在浏览器里直接验证过解法**（只加样式、未改代码）：给 `.pdf-page` 加 `flex-shrink: 0` 后

```
各页高度: 全部 792
内容总高: 12136px（可滚距离 11311px）
滚到底: 渲染第 13/14/15 页，页码显示 15
```

**归属**：这是 **TASK-073 就带着的缺陷**，不是 TASK-081/082 引入的。已在 `b69fde5`
（TASK-081 合并**之前**）上跑同一测试复核，表现一模一样（那时滚到底能到第 7 页，
与用户说的数字吻合）。如实写明，不含糊成「优化」。

### 目标

1. **占位页占住它该占的高度**：15 页全部 792px，总滚动高度约 12136px，滚到底能渲染到最后一页。
2. **顺带核实同一根因的连带影响并如实处置**：JS 侧的 `offsets` 按「每页高×scale + 16」算，
   而真实 DOM 此前是塌的——**页码判定、跳页、位置记忆三样都建立在这个错误的几何上**。
   修正几何后逐一实测：若随之回正，记录数字；若仍不对，属本任务范围，一并修。
3. **补一条真能看住它的守卫**：这是布局缺陷，jsdom 量不出高度，**单测天然抓不到**——
   必须落在 e2e（真实 Chromium）里，断言「滚到底能到最后一页」。

### 非目标 / 禁止范围

- 不改渲染密度（TASK-082 刚定）、不改工具条版式（TASK-081 刚定）。
- 不改 PDF 的取字节与失败分因。
- 不改 `NEAR_PAGES`——后半本不渲染的原因是几何错了，不是这个窗口太小。

### 主 Agent 登记的实现决定（非用户决定，Review 可挑战）

- **用 `flex-shrink: 0` 而不是改成 `display: block`**：容器的 `align-items: center`（页面水平居中）
  与 `gap: 16px`（页间距，且 JS 的 `offsets` 正是按这个 16 算的）都依赖 flex 布局，换掉代价更大。
- **e2e 用真实的多页夹具**：现有 `e2e/pdf-reader.spec.ts` 的夹具只有 2 页，**压不出这个缺陷**
  （2 页时内容高度未必超出容器）。需要一份页数足够多的合成 PDF。

## 完成条件

- 15 页夹具/真实论文：所有占位页高度等于其 `size.height × scale`，总滚动高度与之相符。实测数字写回。
- 滚到文档末尾能渲染最后一页，页码显示最后一页。**e2e 有断言**（jsdom 抓不到，必须真浏览器）。
- 页码判定、跳页、位置记忆三样实测正确（离开再回来落在原处）。数字写回。
- TASK-073/081/082 既有行为不变：`PdfReader.test.tsx` 全绿、`e2e/pdf-reader.spec.ts` 原有 3 条通过。
- `check_task.py` 必要检查 PASS（frontend）。
- L2：1 位独立只读 Reviewer 审最终 diff。
- **按用户 2026-09-21 的新指令：本次不主动推 PR**，先把结果交给用户检查，确认后再推。

## 上下文包

- 实现：`styles.css` 的 `.pdf-page` / `.pdf-reader-pages`；`PdfReader.tsx` 的 `offsets`、
  `locatePage`、`scrollTopFor`（在 `pdfPosition.ts`）。
- 根因与解法验证见上。
- 检查：`python3 scripts/governance/check_task.py --task docs/tasks/TASK-083-pdf-page-boxes.md --worktree`。

## 实现与测试

- 实现 SHA：登记 `dbfaecf`、范围修订 `c9a85a9`，实现随后。
- 命令与结果：`check_task.py` → **CHECKS PASS**（5 条，frontend 组）。
  前端单测 **326 项**全绿；`e2e/pdf-reader.spec.ts` **4 passed**（原 3 条 + 新增 1 条）。

### 落点

改动本体**只有一条 CSS 声明**：`.pdf-page { flex-shrink: 0 }`，外加一段说清它为什么非有不可的注释。
另新增 12 页的 e2e 夹具与一条守卫用例。没有动任何 TypeScript 逻辑——`offsets`、`locatePage`、
`scrollTopFor` 一个字符没改，它们原本就是对的，**错的是真实 DOM 没有长成它们假设的样子**。

### 实测（真实 Edge，1440×900，15 页的 `1706.03762`）

| | 改前 | 改后 |
| --- | --- | --- |
| 各页高度 | `[791, 791, 791, 17, 17, …]` | **全部 792** |
| 内容总高（`scrollHeight`） | 2834px | **12136px** |
| 可滚距离（`scrollHeight − clientHeight`） | 2009px | **11311px** |
| 滚到底 | 停在第 3 页、只渲染到第 5 页 | **渲染第 13/14/15 页，页码显示 15** |

（初稿把 `scrollHeight`（2834）与「可滚距离」（11311）两个量混在一行里比，虽然「约四倍」的
结论不受影响，但证据表不该这么写——独立 Review F1 指出，已按同口径分两行列。）

### 登记时列为「待核实」的三项连带影响，逐一实测

修正几何后**三项全部随之回正，无需额外改动**：

- **跳页**：跳到第 5/9/12/2 页，页码均正确，该页顶部距容器顶恒为 **17px**
  （= 容器 16px 内边距 + 页框 1px 边框，即页顶正好贴着可视区顶部，符合预期）。
- **滚动跟随**：滚到 25% / 50% / 75% 处，页码分别为 5 / 8 / 12，与 15 页的位置相称。
- **位置记忆**：停在第 9 页往下 400px 处离开、回到资料库再回来 —— 页码 10、
  `scrollTop` **6864 → 6864，偏差 0px**。

### 守卫：只能落在 e2e，且做了变异验证

这是布局缺陷，**jsdom 量不出高度，单测天然抓不到**，所以守卫放在真实 Chromium 里。
新增夹具 `sample-long.pdf`（12 页、每页 300×400，手写对象与真实 xref 偏移，照现有
`sample.pdf` 的样子造，不引入依赖也不放真实文献）——**两页的旧夹具压不出这个缺陷**：
两页都在渲染窗口内、都有 canvas 撑着，根本不会出现「没渲染的占位页」。

新用例断言三件事：每个页框（含未渲染的）都占住高度、滚动高度与页数相称、滚到底真能读到第 12 页。

**变异验证**：从 `.pdf-page` 块里去掉 `flex-shrink: 0` → 该用例变红，且现场数字与用户报的现象
一模一样：`各页高度 [399, 399, 399, 18, 18, …]`、`scrollHeight 1568`（应 5008）。还原后复绿。

**这里我先做错过一次，如实记下**：第一次变异用的脚本是「替换文件里第一处 `flex-shrink: 0`」，
而那一处在 `styles.css:52` 的 `.icon` 上（初稿把行号写成 104，也是错的——独立 Review F2 指出）
——`.pdf-page` 的那条原封不动，于是测试照样 4 passed，我差点据此认为「守卫没绑住」。
改成按 `.pdf-page` 块精确定位后才得到上面的真结果。
教训：**变异脚本本身也要确认改对了地方**，不能只看测试结果。

### 已知限制 / 未完成项

- **`offsets` 漏算容器的 `padding-top: 16`、且给末页也加了 16**（独立 Review N1，属 TASK-073 既有）：
  `tops` 因此统一少 16px——「跳页后页顶恒距容器顶 17px」正是它的表现。本次未放大也未修正；
  它不影响可达性与位置记忆的往返一致性（两端用同一套公式，误差抵消），故记录后继续。
- **新增 e2e 的第三条断言隐含依赖视口高度**（独立 Review N2）：中线判据要求 pages 容器
  `clientHeight ≤ 864`，当前约 600px 有余量；将来若调大 e2e 视口，这条可能非缺陷性变红。
- **窄屏下页宽超出容器时，`align-items: center` 会让左侧滚不到**（TASK-073 既有，本次未触及）。
- e2e 夹具每页 300×400pt，比真实论文小；它验的是**几何是否塌陷**这一件事，不代表所有页面尺寸。
- 实测只在 1440×900 与 e2e 的默认视口两档做过；极窄视口未单独验（容器更矮，塌陷压力更大，
  方向上只会更容易暴露问题，不会掩盖它）。

<!-- EVIDENCE:BEGIN -->
## 状态与最终证据

- **最终候选 SHA**：`a264a035f12f9f692468f703fa878e56ad4e44a5`（基线 `168830e`），外加本次按 Review 订正记录的提交。
- **Review**：L2 独立只读 Reviewer 一轮，**PASS，无必须修复项**，2 条非阻断 + 2 处记录措辞订正。报告核心结论原文：

> **① 修法正确且最小。** column flex 下 `flex-shrink` 只管主轴＝高；宽度是交叉轴，由 `align-items: center` + 内联 `width` 决定，本次一字未动 → 窄屏/缩放下**不会**新增横向滚动条或裁剪。容器 `overflow: auto` 且**无** `justify-content: center`（有的话顶部会滚不到），极矮容器只是滚得更长。`display: block` 的理由成立。`box-sizing: border-box`（styles.css:20）使内联 height 与 `offsets` 对齐，无 2px/页漂移。
>
> **② 无他处依赖旧几何。** 全仓只有 `PdfReader.tsx:225-236` 的 `offsets`（纯算术）消费这套几何，`readerPosition/outline` 属网页正文那套；没有为塌陷写的补偿代码，`locatePage` 的中线判据是为短文档而设，不会过度补偿。*N1（非阻断，TASK-073 既有）*：`offsets` 漏了容器 `padding-top:16` 且给末页也加 16 → `tops` 统一少 16px；记录里「跳页后页顶恒距容器顶 17px」正是它，本次未放大。
>
> **③ e2e 确实绑住行为。** 夹具 12×400=4800 ≫ 容器（Desktop Chrome 1280×720），初始只渲染 1-3 页，必有 9 个占位页，复现前提成立。①有 `tallest>100` 兜底（整体等比塌陷约 49px 也会红）；②`scrollable>3200` vs 实际约 4400，且容器若长成内容高（scrollable=0）同样红；③是真绑定。*N2（非阻断）*：③ 隐含依赖视口——中线判据要求 pages 容器 `clientHeight ≤ 864`。现约 600px 有余量，将来调大 e2e 视口会非缺陷性变红。
>
> **④ 夹具自洽，逐字节核过。** 头 9B；obj1→9、obj2→58、obj3→187；单位数对象步长 128、双位数 129；内容流 99/100；末对象止于 **2990 = startxref**；`/Size 28`、`/Count 12`、Kids 12 项、xref 每条 20B、0 号 `f` 条目、`%%EOF` 齐全，总长 3616B 与 3.5KB 相符。不依赖解析器容错，无跨 pdf.js 版本偶发风险。
>
> **⑤ 记录基本如实，两处措辞需纠正。** **F1**「总可滚高度 2834 → 11311」两数不同量——11311 = scrollHeight−clientHeight，2834 是 scrollHeight。**F2**「第一处 `flex-shrink: 0` 在第 104 行」不准——文件里第一处在 `styles.css:52`（`.icon`）。自省叙述本身可信：按 `.pdf-page` 复算变异现场 32+3×399+9×18+11×16=1567≈记录的 1568。归属 TASK-073 **成立**。三项连带影响**可复算**：25/50/75%→5/8/12（tops 步长 808）、跳页 17=16 padding+1 border、6864=8×808+400，均与代码公式吻合，非空口。

**F1 与 F2 已按原文订正**（见上文实测表与自省段），N1/N2 已进「已知限制」。

- **最终状态：MERGED**——用户 2026-09-21 合并 PR #91，merge 提交 `36e09e2`。**用户已于 2026-09-21 亲自检查通过**——原话：
  「可以，我又爬了一篇残差学习网络的论文，也可以看完。」（按用户当日新指令：先给他检查、确认后才推 PR。）
- **剩余风险**：窄屏下页宽超容器时左侧滚不到（TASK-073 既有）；夹具仅 300×400，不代表大幅面页；
  `offsets` 少 16px 的既有偏差未修。
- **非阻断遗留项**：见「已知限制 / 未完成项」四条。
- 日期与决定日志：2026-09-21 用户报「只能出现前 7 页」→ 主 Agent 实测定位到占位页被 flex 压成 17px、
  并在浏览器里验证 `flex-shrink: 0` 可解 → 复核确认缺陷属 TASK-073 而非新近两次改动 → 用户「解决新任务吧」
  → 登记 TASK-083。
<!-- EVIDENCE:END -->
